/**
 * 
 *  sort.c
 *
 * J. Bobula
 * Exit32 Studio
 * contact@exit32studio.com
 *
 * Contains various sort methods.  Each sort method functions the same; pass in an array of number
 * and the method will sort the numbers in the array 'destructively'.  In other words, it will replace
 * the passed array with a new, sorted array.
 *
 **/
 
 #include <stdio.h>
 
 /**
  *
  * Uses a linear sort on the passed 'haystack', which is an array of ints.
  *
  **/
 void linearSort(int *haystack []);
 
 
 
 int main (int argc, char* argv [])
 {
 }
 
